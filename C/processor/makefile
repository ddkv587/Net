include $(CLEAR_VARS)

TARGET_MODULE := Processor

CPP_INCLUDE		:= -I$(ROOT_PATH) \
				   -I.
CPP_SRCDIR		:= .
CPP_SRC			:= $(shell find $(CPP_SRCDIR) -name "*.cpp")

LIBS			+=
LIBS_DIR		+=

include $(BUILD_OBJ)
